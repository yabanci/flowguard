linters:
  enable:
    - errcheck
    - gosec
    - govet
    - staticcheck
    - unused

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
    - path: benchmarks/
      linters:
        - errcheck
